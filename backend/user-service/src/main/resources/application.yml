server:
  port: 8081 # 用户服务端口

spring:
  application:
    name: user-service # 定义服务名
  datasource:
    url: jdbc:mysql://localhost:3306/ai_customer?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 303600677gby
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: "303600677gby"
  expiration: 604800000